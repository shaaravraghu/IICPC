[package]
name = "strategy-parser"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
platform-types = { path = "../platform-types" }

[lib]
path = "src/lib.rs"
